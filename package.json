{
  "name": "smd-model-validation",
  "private": true,
  "scripts": {
    "test": "npm run validate:models",
    "validate:models": "node scripts/validate-models.js"
  },
  "devDependencies": {
    "ajv": "8.20.0"
  }
}
